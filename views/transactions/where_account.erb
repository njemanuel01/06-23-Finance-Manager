<h1>Which account do you want to see a list of transactions for?</h1>

<div class="menu">
  <ul>
    <li><a href="/new_transaction">Create a transaction</a></li>
    <li><a href="/all_transactions">See a list of all transactions and information on them.</a></li>
    <li><a href="/where">See a list of transactions for a specific account</a></li>
    <li><a href="/update_transaction_form">Update a transaction's information</a></li>
    <li><a href="/delete_transaction_list">Delete a transaction</a></li>
  </ul>
</div>

<div class="content">
  <form action="/where">
    <select name ="id">
      <% Account.all.each do |account| %> 
        <option value="<%= account.id %>"><%= account.id %></option>
      <% end %>
    </select>
    <input type="submit">
  </form>

  <% if params["id"]%>
    <h1>Here they are. Click on a transaction to see it's full details.'</h1>

    <ul>  
      <% Transaction.where("account_id", params["id"]).each do |transaction| %>
        <li><a href="/single_transaction/<%=transaction.id%>"><%= transaction.id %> - <%= transaction.amount%> - <%= transaction.date%> - <%= transaction.description%></li>
        <% end %>
    </ul>
  <% end %>
</div>

<div style="clear: both"></div>