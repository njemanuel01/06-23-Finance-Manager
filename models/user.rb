class User
  def initialize(values = {})
    @id = values["id"]
    @name = values["name"]
  end
  
  def total_balance
    #method that adds the balances of all the accounts associated with the user
  end
  
end